extends layout

block content
  .container
    .row
      .col-md-3
      .col-md-6
        form#form(role='form')
          label(for='Input') Email the thing
          input#Input.form-control(type='text', placeholder='Enter', name='sr')
        #todolist
          p 正在进行
          ul#upul
             - each item in items
              li= item
        #donelist
          p 已完成
          ul#downul
            - each item in bitems
             li= item
      .col-md-3

